server:
  port: 0

spring:
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      fail-fast: false
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db
    username: postgres
    password: "09282325"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          # Esta URL debe ser alcanzable por el microservicio.
          # En local es localhost:8080. En Docker será http://keycloak:8080...
          jwk-set-uri: http://localhost:8080/realms/sisgr-realm/protocol/openid-connect/certs