server:
  port: 8761 # <--- AGREGA ESTO OBLIGATORIAMENTE

spring:
  application:
    name: eureka-service
  cloud:
    config:
      uri: http://localhost:8081
      fail-fast: false

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false