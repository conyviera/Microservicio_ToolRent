apiVersion: v1
kind: Service
metadata:
  name: kardex-service
spec:
  selector:
    app: kardex-service
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 0 # Soporte para asignación dinámica (server.port=0)
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kardex-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kardex-service
  template:
    metadata:
      labels:
        app: kardex-service
    spec:
      containers:
        - name: kardex-service
          image: kardex-service:latest
          imagePullPolicy: Never
          env:
            - name: SERVER_PORT
              value: "0" # REQUISITO RÚBRICA: Puerto dinámico
            - name: SPRING_CONFIG_IMPORT
              value: "optional:configserver:http://config-service:8888"
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://eureka-service:8761/eureka/"
            # CONEXIÓN EXCLUSIVA A LA DB kardex_db (Punto 2 Rúbrica)
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-db:5432/kardex_db"
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgres"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "09282325"